**트리거:** 정산 세션에서 사용자가 **"확정됐어"** 한마디만 하면, 그 시점 최신 확정 출고일 정산(COGS 확정 + 한미 실배송비 확정 + 순이익 계산 완료된 `output/settlement/정산_<date>.md`)을 **되묻지 말고** Notion 페이지에 추가한다.

**대상 페이지:** [2026 7월부터 정산](https://app.notion.com/p/3a0fd6ea54b780afb8f8d978c44e18b1) (부모: 핀치마트)
**데이터베이스:** `제품별 주문 데이터` — `collection://02248850-4d52-42ae-a024-e81089dd0091` (페이지 하위, is_inline=true, 최상단 배치)

Slack DM([[feedback_settlement_slack_dm]] §20-6)과 **별개 채널** — Notion 추가가 DM을 대체하지 않음. 둘 다 필요하면 각각 수행.

---

## 1. 페이지에 넣는 것 (매 확정마다)

1. **DB에 그 배치 제품 행 추가** (`notion-create-pages`, parent = data_source_id) → 차트 자동 누적.
2. **"출고일별 정산 요약" 표에 그 출고일 행 추가** (매출·COGS·실배송비·순이익·상태).
3. **날짜별 상세 섹션 추가** (매출/수취인별 표/COGS 목록/손익) — [[project_order_settlement]] 형식 그대로.

이미 그 출고일이 있으면 중복 추가 말고 스킵/갱신.

**🔑 날짜별 상세 섹션 = 주문별 순이익까지 한 테이블 (한미 무게별 실배송비 기준, 균등분배 금지):** 섹션 상단에 **정산 요약 표**(매출·물건값COGS·실배송비·순이익)를 별도로 두고, 이어 **수취인별 표**에 `수취인 · 제품정산 · 배송정산 · 소계 · 물건값(COGS) · 제품손익(소계−COGS) · 무게(kg) · 실배송비(한미 무게별) · 순이익(제품손익−실배송비)` 컬럼 전부. 실배송비는 한미가 준 수취인별 무게로 §7-1 무게식 `$9+$4.5×(무게−1kg)`(ceil 안 함, 연속값) ×FX 계산 → **8명(주문) 합이 한미 최종 청구 총액과 일치 검산**. 균등분배(총액÷묶음수) 쓰지 말 것 — 저가 소액주문을 적자로 왜곡(7/10 윤지애 균등분배 −₩2,798 → 무게실측 +₩1,466 반전). 무게 미제공 시에만 상품 실중량으로 무게식 추정 배분, 균등분배는 최후. 사용자: "7/14, 17일도 한미 오피셜 나오면 그렇게 만들어 계속!" — 매 출고일 반복. (LEARNED §20-11)

**페이지 레이아웃:** DB(차트) 최상단 → 출고일별 요약 표 → 날짜별 상세.

## 2. DB 스키마 · 차트 (확정형)

| 컬럼 | 타입 | 내용 |
|---|---|---|
| 제품명 | TITLE | 브랜드+라인명+타입+옵션+용량 (§3 규칙) |
| 개수 | NUMBER | **주문 개수** (금액 아님, §4 규칙) |
| 출고일 | DATE | 그 배치 출고일 |
| 월 | RICH_TEXT | `2026-07` — 차트 stack 축 |

**차트 = "제품별 주문수" 탭 하나뿐.** GROUP BY 제품명 · AGGREGATE count · 월 컬럼으로 stack.

**🔑 Notion 무료 플랜 = chart view 1개 제한.** 월별로 DB/차트를 새로 만들지 말고 **이 DB 하나에 모든 달을 계속 누적**(그래서 제목에 월 표기 없음 — 월은 `월` 컬럼으로 구분). **새 chart view 를 `create-view` 로 추가 시도 금지** — 한도 걸리고 사용자가 UI에서 지워야 함.

**차트 = 금액이 아니라 주문 빈도.** COGS SUM 기준으로 시도했다가 "Something is wrong with your chart data" 렌더 실패 + 사용자 정정("제품별 주문수로 해야할거같은데"). 금액은 정산 페이지 본문 COGS 섹션에 이미 있으니 DB에 중복 보관 안 함.

## 3. 제품명 규칙 — 축약 금지

정산 md 자체가 축약형이어도 **DB엔 정확한 풀네임**을 넣는다 (사용자: "이렇게 봐선 정확히 뭔지 모름").

- ① **옵션/변형(맛·향·색상) 포함**, 서로 다른 옵션은 **행 분리** — `미스비키스 감자칩 - 올드레스드` / `미스비키스 감자칩 - 할라피뇨` (한 행에 "올드레스드+할라피뇨" ❌)
- ② **브랜드+라인명+타입 다 포함, 임의 축약 금지** — `타이드 세탁캡슐 63입` ❌ → `타이드 파워포드 스포츠 세탁캡슐 63입` ✅
- ③ **등록 산출물 있으면 확정 명칭 사용** — 용량(g/ml)으로 `output/new-item/<slug>_<용량>/*_product_info.json` grep → `title_ko` 채택. `다우니 퓨전 598g` ❌ → `다우니 언스토퍼블 퓨전 트와일라잇자스민 598g` ✅
- ④ 내부용 메모는 빼기 — `애프터바이트(무료취득)` ❌ → `애프터바이트` ✅ (글리치·무료취득은 정산 본문에만)

## 4. 🔑 `개수` = 실제 주문량(수량) — 주문건수(제품당 1) 아님 (2026-07-17 명확화)

`개수`는 **발주발송 엑셀 `수량` 컬럼값**(주문량)을 그대로 넣는다. 같은 옵션 여러 개 산 것은 **그 수량**(각 1 아님).

- Gerber 파우치 맛별 5·5·8·6·6 → 맛별 행 분리 + 각 행에 **그 수량**(5,5,8,6,6). 네스카페 김하나8+이재중10 → 개수 **18**. 커클랜드 3병 → **3**, 딥릴리프 500g×2 튜브 → **2**.
- 서로 다른 맛/옵션은 **행 분리**하되 각 행에 **그 옵션의 실제 수량**.
- **예외 = 진짜 세트/묶음 SKU만 세트 1개=개수 1:** 판매단위가 애초에 묶음인 것 — `세라비 453ml 2개입 세트`(1) · `웨버 조인트케어 2개 묶음세트`(1). 원가 `$29.99×2` 는 세트 구성이라 주문수량 오독 금지. **낱개를 수량 N개 담은 건 개수 N**(딥릴리프 튜브 2개=2).
- 구분: "리스팅이 N개 묶음으로 팔리나?"→세트(1) vs "낱개를 수량 N 담았나?"→N.

> 2026-07-17: Gerber 5맛(30개)·네스카페(18)·오레오5·치즈위즈5·커피크리스프6·베르가못2·커클랜드3·딥릴리프2를 전부 주문건수(1 또는 2)로 넣었다가 사용자 "주문건수로 처리한거야? 주문량으로 한게 아니라?" 지적 → 엑셀 `수량` 컬럼에서 실수량 뽑아 12행 정정.

**⚠️ 차트는 count 만 됨:** MCP DSL `AGGREGATE` 가 sum-of-property(개수 합계)를 못 받아(파싱 실패) 차트는 **행 개수(count)** 기준 — 개수 컬럼값 미반영. 주문량 막대로 보려면 **UI 에서 집계를 "개수 합계(Sum)"로 수동 변경**. 수치 정답은 테이블 `개수` 컬럼. 헤딩/캡션도 "주문수"→"주문량".

## 5. Notion MCP 실전 노하우 (시행착오 정리)

- **차트 DSL** (통과하는 유일 문법): `CHART bar AGGREGATE "count" GROUP BY "제품명"` — **세미콜론 없이 공백으로 이어 쓰고**, AGGREGATE 뒤엔 **함수명을 소문자 따옴표**(`"count"`/`"sum"`, 프로퍼티명 아님). `SORT BY` 는 quoted 프로퍼티만 허용 — count 자체로는 정렬 불가.
- **차트를 페이지에 심는 법:** `?v=<view-id>` 쿼리스트링은 실패(Notion 이 base uuid 같은 `<database>` 태그를 동일 블록으로 병합/dedup → `inline="false"` 링크로 collapse). **DB 자체를 `is_inline: true`**(`notion-update-data-source`) 로 두고 `<database inline="true">` 블록을 원하는 위치에 배치하는 게 정답. (`create-view(parent_page_id=…)` 로 만들면 페이지 끝에 붙는데, 그 url 은 **페이지를 다시 fetch 해서 새로 붙은 블록의 url 을 읽어야** 함 — 응답의 `view://<uuid>` 를 dash 제거해 쓰면 틀림.)
- **`replace_content` 로 페이지 재구성 시** 유지할 하위 DB/페이지를 새 content 에서 빠뜨리면 "하위 페이지/DB 삭제" 에러 → 유지할 건 `<database url="…" data-source-url="…">이름</database>` 태그로 포함, 진짜 지울 거면 `allow_deleting_content: true`.
- **컬럼 리네임:** `notion-update-data-source` statements `RENAME COLUMN "A" TO "B"`. 차트 config 가 GROUP BY 제품명 + count 뿐이라 숫자 컬럼 리네임은 차트를 안 깨뜨림.
- **폐기된 접근:** SVG 이미지 첨부(`notion-create-attachment` + `<image>`) 방식은 사용자 요청("svg말고 노션 차트로")으로 폐기, 파이차트도 미채택. 네이티브 DB+chart view 만 사용.

## 6. 🔑 매월 1일(이후 첫 세션) = 전달 월간 누적 정산도 같은 페이지에 추가 (2026-07-17)

"확정됐어" 트리거(위 1~5)는 **출고일 배치 단위** 추가다. 이와 별개로 **매월 1일이 되면(또는 그 이후 첫 세션에서)** 전달 전체를 합산한 **월간 누적 정산**도 같은 페이지에 추가한다. 사용자 지시: "노션에 8월 1일 되면 월 정산도 해주고".

- **적용 조건:** 그 달의 모든 출고일 정산(`정산_<date>.md`)이 COGS·실배송비까지 전부 확정돼야 포함. 미확정 배치는 제외하고 사용자에게 안내(임의 추정치 끼워넣기 금지).
- **추가 위치:** "월별 정산 요약" 섹션(표) — 월·매출합계·COGS합계·실배송비합계·순이익합계. §20-8의 "출고일별 정산 요약" 표와는 별개.
- **첫 적용:** 2026-08-01 이후 세션에서 2026년 7월 전체(7/10·7/14·7/17 등) 누적, 이후 매월 반복.

## 7. 🚨 노션 업데이트 후 항상 Codex 독립 QA로 엔트리 검증 (2026-08-01, 새 규칙)

노션 정산/DB를 업데이트할 때마다(위 1~6의 "확정됐어" 트리거 포함, 그 외 수동 갱신도 동일) **두 단계를 세트로 한다**:
① 노션 업데이트 ② **Codex를 읽기전용 독립 QA로 불러 방금 넣은/바꾼 엔트리를 검증**받는다. 특히 `제품별 주문 데이터` DB처럼 `GROUP BY 제품명`으로 차트를 그리는 곳은, 같은 SKU인데 제품명 문자열이 미묘하게 다르게 들어간 게 없는지 Codex가 전수 확인하고, 있으면 같은 표기로 통일해서 합친다.

**절차 (Codex는 Notion MCP 접근이 없으므로 반드시 로컬 파일 경유):**
1. `notion-query-data-sources` 로 DB 전체 행(제품명·개수·출고일·**월** 등 스키마의 모든 프로퍼티)을 로컬 파일(`url|제품명|개수|출고일|월` 파이프 구분)로 export — 특정 컬럼만 뽑지 말고 전체 프로퍼티를 포함시킬 것.
2. CLAUDE.md 상단 Codex 규칙대로 `codex exec --sandbox read-only --skip-git-repo-check -` heredoc 방식으로 호출, 그 파일 경로를 프롬프트에 주고 **두 가지 다** 찾으라고 시킨다: (a) 동일 SKU인데 제품명 표기가 다른 그룹(canonical 이름 제안, 판단 기준: 브랜드+제품명+용량+옵션 동일=같은 SKU, 사소한 표기차는 합치되 실제 옵션/용량이 다르면 별개) (b) `월` 등 텍스트/날짜 프로퍼티의 포맷 불일치(같은 의미인데 다른 문자열 — 예 `"2026-08"` vs `"2026년 8월"` vs `"8월"`, 다수/canonical 값과 소수 이상치 보고).
3. Codex가 찾은 그룹마다 각 페이지를 `notion-update-page`(`update_properties`)로 canonical 값으로 통일 — 행을 지울 필요 없음, 텍스트만 맞으면 GROUP BY/필터가 정상 작동함.
4. 결과를 요약해 사용자에게 보고.

> 2026-08-01 학습 — 7/28·7/31 배치 노션 반영 후 사용자 "codex한테 노션 엔트리 확인 해달라 해... 똑같은 제품인데 제품명을 멋대로 쓴것도 확인하고 합쳐줘" 지시로 처음 이 절차를 실행. Codex가 9개 그룹(네스카페 리치 헤이즐넛100g·다우니 퓨전 트와일라잇자스민598g·다우니 프레쉬향기1.13kg·다우니 퓨전 선셋앤시트러스598g·도브 센서티브 뷰티바16개·미스비키스 올드레스드·커클랜드 글루코사민300정·타코벨 치폴레소스354ml·팀홀튼 프렌치바닐라카푸치노8개입)의 표기 불일치를 찾아냄 — 위 §3 "매번 완전히 동일한 문자열로" 규칙이 이미 있었는데도 실무에서 계속 새어나간다는 뜻이므로, 사후 Codex 검증을 **매번 고정 절차**로 격상. 애프터바이트(일반) vs After Bite Kids(키즈)처럼 실제로 다른 SKU는 Codex도 합치지 않았음 — 무조건 병합이 아니라 판단 위임.

**🚨 "확정됐어" 라이브 트리거뿐 아니라 뒤늦은 소급 반영(catch-up)에서도 똑같이 샌다 (2026-08-11):** 8/7 배치가 로컬 md에서는 "최종 확정"까지 끝났는데 세션 중 "확정됐어" 문구가 안 나와 노션 반영 자체를 통째로 빠뜨렸다가, 사용자가 "노션에 8/7 정산 관련이 없는데?"로 지적해 뒤늦게(2026-08-11) DB 10행+recap 섹션을 소급 추가했다. 이때 또 Codex QA를 건너뛰어 "네스카페 리치 헤이즐넛 인스턴트커피 100g"(신규 표기)이 기존 "네스카페 리치 인스턴트커피 헤이즐넛 100g"과 갈라졌고, 사용자가 "제대로 코덱스 QA안돌리냐"로 재차 지적한 뒤에야 전수 재검증(8개 그룹 통일: 홀리스타 밀크씨슬·콜게이트치약·거버3종·룰루레몬헤드밴드·PC쿠키·호올스)을 돌렸다. **교훈: DB에 행을 추가하는 경로가 라이브 확정이든 소급 캐치업이든 상관없이, 추가 직후 반드시 Codex QA — "이번엔 특수 케이스니까 나중에" 예외 금지.**

**🚨 (c) 자유서술 프로즈(§20-6-1 "특이사항")의 오탈자·비문도 Codex QA 대상 — 셀프 검토 불신 (2026-08-11):** 위 (a)(b)는 DB 행(구조화 데이터)만 본다. `스토어 구매패턴 & etc` 페이지 §7 "기타 관찰"처럼 **그 자리에서 쓴 한국어 자유서술문**은 별개 취약점 — 오탈자(`추가해둥`→`추가해둔`)·오기(`얼혀져`→`얽혀져`, `마진 얼은`→`마진 얇은`)·비문(`저평가될 미연을 미연에 잡음` 같은 단어 중복)을 셀프 검토 없이 그대로 올렸다가 사용자가 "노션 이거 오탈자 확인 안하고 그냥 올렸냐?"로 직접 잡아냄. 사용자: "이것도 너 못맡기겠어.. codex qa 친구 해야할 리스트에 적어놔". → 프로즈를 노션에 쓸 때(§20-6-1 배치 특이사항 등)도 Codex QA에 **(c) 항목을 추가**: 방금 쓴/수정한 문단을 그대로 넘겨 "오탈자·오기(음운 유사 오타)·비문(단어/구 중복, 주어-서술어 불일치)만 지적하라"고 시킨다(내용·판단은 안 건드림, 순수 교정). 지적된 부분만 원문 대조 후 `update_content`로 고친다. DB 행 QA가 필요 없는, 프로즈만 올리는 세션에서도 (c)는 생략 금지.

[[project_order_settlement]]
